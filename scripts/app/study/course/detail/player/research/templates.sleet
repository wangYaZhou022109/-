#!handlebars

module
    div(data-region='main')

view('main')
    .layout-mt-small.text-center(style="color:#fff;")
        p.text-center 问卷须知
        p.text-center.layout-mt-small > echo(researchActivity.questionaryDetail)
        .btn.radius.layout-mt#submit 进入调研