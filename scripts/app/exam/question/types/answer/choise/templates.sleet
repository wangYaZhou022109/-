#!handlebars

module > .choise.preview-content
    div(data-region='content')
    .options(data-region='options')

view('content')
    p.question > @echo(state.content)


view('options') > .answer
    .preview-list > dl.border.bottom > each(options)
        dd(id='item-' + index)
            if(mutiple)
                .checkbox.no-margin.inline-block
                    input(type='checkbox' id='check-'+@index value=@index)('checked')&if(checked)
                    label(for='check-'+@index)
            else
                input.inline-block(type='radio' name='value-' + questionId id='radio-'+@index value=@index)('checked')&if(checked)
            .inline-block.m-left(for='answer-item-' + index)
                span.answer-options > span.option-num > @echo(code)
                span.answer-options > @echo(content)

    if(state.detailMode)
        .show-answer.border.border-top
            .row
                .col-xs-6 > .com-title > echo('答案：' state.answer)
                .col-xs-6
                    span > echo('本题得分：')
                    span > restoreNumber(state.gainScore 100)
                    span > echo('本题易错率：')
                    span > restoreNumber(state.errorRate 10000) > echo('%')
