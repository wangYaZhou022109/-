#!handlebars

module > .choise
    div(data-region='content')
    .options(data-region='subs')


view('content')
    p.question
        span > echo(state.index '：')
        @echo(state.content)

view('subs') > .m-bottom
    each(subs)
        div(data-dynamic-key=id)
