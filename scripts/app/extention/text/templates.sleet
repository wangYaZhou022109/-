#!handlebars html

module
    div(data-region='text')

view('text')
    label(style='display: none;')&if(config.notShow) > echo(config.name)
    input.input(x-rule='required')&if(config.isRequired)(type='hidden')&if(config.notShow)(value=entity.text name=inputName)
