#!handlebars

module
    .content(data-region='tags')
    .content(data-region='main')
view('tags')
    #tags
view('main')
    .clearfix
        .pull-right > .tag-btn.radius.no-margin#selectTopic > i.iconfont.icon-more
        .sub-row.overflow.text-overflow#tag-list
            .inline-block.small-font 推荐标签：
            each(hot)
                .tag-btn.radius(id='hot-'+id)(class = if(selected 'selected')) > echo(name)
