#!handlebars

module
    .content(data-region='main')

view('main')
    #tags
    .sub-row#tag-list
        span.small-font 推荐标签：
        each(hot)
            .tag-btn.radius(id='hot-'+id) > echo(name)
        # .tag-btn.radius.selected .selected
        .tag-btn.radius#selectTopic ...