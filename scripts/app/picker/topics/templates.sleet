#!handlebars

module
    .content(data-region='tags')
    .content(data-region='main')
view('tags')
    #tags
view('main')
    .clearfix.topics-list
        .topics-right > .tag-btn.radius.no-margin#selectTopic > i.iconfont.icon-more
        .sub-row.overflow#tag-list.pull-left
            .small-font.pull-left 推荐标签：
            each(hot)
                .tag-btn.radius.pull-left(id='hot-'+id)(class = if(selected 'selected')) > echo(name)
