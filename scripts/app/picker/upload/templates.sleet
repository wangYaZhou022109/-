#!handlebars

module
    div(data-region='main')

view('main')
    .user-pic > img(src = downUrl)
    input#headImageId( type='hidden' name=state.inputName value=img.imgId)
    .btn#select > i.icon-plus + span > string('picker.upload.label.select')


view('upload') > #uploader
