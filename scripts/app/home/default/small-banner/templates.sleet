#! handlebars
module
    .min-banner(data-region='min-banner')

view('min-banner') > .container
    #swiper > ul > each(smallBanners)
        li > a(href=linkAddress)(target='_blank')&if(outerLink) > img(src=downloadUrl(pcImage))
