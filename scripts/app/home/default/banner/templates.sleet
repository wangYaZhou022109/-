#!handlebars

module
    #banner(data-region='advertising')

view('advertising')
    #banner.banner > ul
        each(adverts)
            li > img(src=../downUrl + pcFilePath)
        else
            li
