#!handlebars

module
    div(data-region='main')

view('main')
    each(announcements)
        .clearfix.layout-mb-small
            .h3.m-bottom > echo(title)
            p > @echo(content)
            .pull-right
                .sub-text 来自XXXXX
                .text-tips.m-top > dateTime(lastModifyTime)
        .hr.dashed
