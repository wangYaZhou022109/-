#!handlebars

module > .exam-verify-page
    .exam-verify(data-region="exam-verify")

view('exam-verify')
    .layout-mt-small
        label 请假原因
        textarea.w-full(rows="5")
        .m-top.clearfix.m-bottom
            .pull-left
                label 审核意见：
                select
                    option 不同意
                    option 同意
            .pull-right.r-block > span 申请时间：2017-02-20 09:44
